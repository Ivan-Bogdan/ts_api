export let ENV = {
    // @@@env file contains environment Variables
    // debugMode:true,
    // @param {debugMode} responsible for logging
    // dataBase:"exampelBase" database name
    // @param  {dataBase} database name
}